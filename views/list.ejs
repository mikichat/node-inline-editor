<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HTML 파일 목록</title>
    <style nonce="<%= nonce %>">
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
            min-height: 100vh;
            color: #334155;
            padding: 40px 20px;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
        }

        h1 {
            text-align: center;
            margin-bottom: 40px;
            font-size: 2.5rem;
            background: linear-gradient(90deg, #4f46e5, #0ea5e9);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            /* text-shadow: 0 0 30px rgba(79, 70, 229, 0.1); */
        }

        .file-list {
            background: #ffffff;
            border-radius: 16px;
            border: 1px solid #e2e8f0;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
        }

        .file-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 20px 30px;
            border-bottom: 1px solid #f1f5f9;
            transition: all 0.3s ease;
        }

        .file-item:last-child {
            border-bottom: none;
        }

        .file-item:hover {
            background: #f8fafc;
        }

        .file-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .file-icon {
            width: 45px;
            height: 45px;
            background: linear-gradient(135deg, #4f46e5, #0ea5e9);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            color: white;
        }

        .file-name {
            font-size: 1.1rem;
            font-weight: 600;
            color: #1e293b;
        }

        .file-meta {
            font-size: 0.85rem;
            color: #64748b;
            margin-top: 4px;
        }

        .btn-edit {
            padding: 10px 28px;
            background: linear-gradient(135deg, #4f46e5, #0ea5e9);
            border: none;
            border-radius: 25px;
            color: #ffffff;
            font-weight: 600;
            font-size: 0.95rem;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .btn-edit:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(79, 70, 229, 0.3);
            filter: brightness(1.1);
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #94a3b8;
        }

        .empty-state p {
            font-size: 1.2rem;
            margin-bottom: 10px;
        }

        .empty-state span {
            font-size: 0.9rem;
            color: #cbd5e1;
        }

        /* 검색 바 스타일 */
        .search-container {
            margin-bottom: 30px;
        }

        .search-wrapper {
            position: relative;
            display: flex;
            align-items: center;
        }

        .search-icon {
            position: absolute;
            left: 20px;
            font-size: 1.1rem;
            color: #94a3b8;
            pointer-events: none;
        }

        #fileSearch {
            width: 100%;
            padding: 15px 15px 15px 50px;
            background: #ffffff;
            border: 1px solid #e2e8f0;
            border-radius: 12px;
            font-size: 1rem;
            color: #1e293b;
            transition: all 0.3s ease;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
            outline: none;
        }

        #fileSearch:focus {
            border-color: #4f46e5;
            box-shadow: 0 10px 15px -3px rgba(79, 70, 229, 0.1);
        }

        #fileSearch::placeholder {
            color: #94a3b8;
        }

        .no-results {
            display: none;
            text-align: center;
            padding: 40px 20px;
            color: #94a3b8;
        }

        .no-results p {
            font-size: 1.1rem;
            margin-bottom: 5px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div style="display: flex; justify-content: flex-end; margin-bottom: 10px;">
            <a href="/logout" style="color: #ff6b6b; text-decoration: none; font-size: 0.9rem; font-weight: 500;">🔒
                로그아웃</a>
        </div>
        <h1>📄 HTML 파일 목록</h1>

        <div class="search-container">
            <div class="search-wrapper">
                <span class="search-icon">🔍</span>
                <input type="text" id="fileSearch" placeholder="파일 이름으로 검색..." autocomplete="off">
            </div>
        </div>

        <div class="file-list" id="fileList">
            <% if (files.length===0) { %>
                <div class="empty-state">
                    <p>HTML 파일이 없습니다.</p>
                    <span>public 폴더에 .html 파일을 추가해주세요.</span>
                </div>
                <% } else { %>
                    <% files.forEach(file=> { %>
                        <div class="file-item">
                            <div class="file-info">
                                <div class="file-icon">📝</div>
                                <div>
                                    <div class="file-name">
                                        <%= file.name %>
                                    </div>
                                    <div class="file-meta">
                                        <%= (file.size / 1024).toFixed(2) %> KB ·
                                            <%= new Date(file.mtime).toLocaleString('ko-KR') %>
                                    </div>
                                </div>
                            </div>
                            <a href="/editor?file=<%= encodeURIComponent(file.name) %>" class="btn-edit">수정</a>
                        </div>
                        <% }); %>
                            <div id="noResults" class="no-results">
                                <p>검색 결과가 없습니다.</p>
                                <span style="font-size: 0.85rem; color: #cbd5e1;">다른 검색어를 입력해 보세요.</span>
                            </div>
                            <% } %>
        </div>
    </div>
    <script nonce="<%= nonce %>">
        // 파일 검색 기능
        const searchInput = document.getElementById('fileSearch');
        const fileItems = document.querySelectorAll('.file-item');
        const noResults = document.getElementById('noResults');

        if (searchInput) {
            searchInput.addEventListener('input', function (e) {
                const query = e.target.value.toLowerCase().trim();
                let visibleCount = 0;

                fileItems.forEach(item => {
                    const fileName = item.querySelector('.file-name').textContent.toLowerCase();
                    if (fileName.includes(query)) {
                        item.style.display = 'flex';
                        visibleCount++;
                    } else {
                        item.style.display = 'none';
                    }
                });

                if (noResults) {
                    noResults.style.display = (visibleCount === 0 && fileItems.length > 0) ? 'block' : 'none';
                }
            });
        }

        // 유휴 시간 자동 로그아웃 (10분)
        (function () {
            let idleTimer;
            const idleLimit = 10 * 60 * 1000; // 10분

            function resetTimer() {
                clearTimeout(idleTimer);
                idleTimer = setTimeout(() => {
                    alert('10분간 활동이 없어 보안을 위해 자동 로그아웃되었습니다.');
                    window.location.href = '/logout';
                }, idleLimit);
            }

            // 활동 감지 이벤트
            window.onload = resetTimer;
            document.onmousemove = resetTimer;
            document.onkeydown = resetTimer;
            document.onclick = resetTimer;
            document.onscroll = resetTimer;
        })();
    </script>
</body>

</html>